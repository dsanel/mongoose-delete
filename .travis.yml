language: node_js
node_js:
  - 10
  - 8
  - 6
  - 4
services:
  - mongodb
before_script: "npm run add-mongoose"
script: "npm run-script test-travis"
after_script: "npm run add-mongoose && npm install coveralls@3.0.0 && cat ./coverage/lcov.info | coveralls"
